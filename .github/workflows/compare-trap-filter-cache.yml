name: Compare Trap Filter (Live vs Cache)

on:
  workflow_dispatch:
    inputs:
      offset:
        description: "백필 성공 종목 중 시작 인덱스 (0부터)"
        required: true
        default: "0"
      limit:
        description: "이번 실행에서 비교할 종목 수"
        required: true
        default: "50"
      market:
        description: "대상 시장"
        required: true
        default: "ALL"
        type: choice
        options:
          - ALL
          - US
          - KR

jobs:
  compare:
    name: Compare (offset=${{ inputs.offset }} limit=${{ inputs.limit }} market=${{ inputs.market }})
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run comparison
        env:
          TURSO_DATA_URL: ${{ secrets.TURSO_DATA_URL }}
          TURSO_DATA_TOKEN: ${{ secrets.TURSO_DATA_TOKEN }}
        run: >
          python scripts/compare_trap_filter_cache.py
          --offset ${{ inputs.offset }}
          --limit ${{ inputs.limit }}
          --market ${{ inputs.market }}
